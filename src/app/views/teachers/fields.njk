
<div class="item">
  <div>Avatar URL</div>
  <div>
    <input type="url" name="avatar_url" placeholder="http://" required="required" value="{{teacher.avatar_url}}">
  </div>

</div>

<div class="item">
  <div>Nome Completo</div>
  <div>
    <input type="text" name="name" placeholder="Fulano da Silva Sauro" value="{{teacher.name}}">
  </div>

</div>

<div class="item">
  <div>Data de Nascimento</div>
  <div>
    <input type="date" name="birth" value="{{teacher.birth}}">
  </div>

</div>

<div class="item">
  <div>Tipo de Aula</div>
  <div>
    <span>
      <input type="radio" name="classType" value="local" {{'checked' if (teacher.classType != 'distance')}}>
      Presencial
    </span>
    <span>
      <input type="radio" name="classType" value="distance" {{'checked' if (teacher.classType == 'distance')}}>
      À Distância
    </span>
  </div>
</div>

<div class="item">
  <div>Nível de Escolaridade</div>
  <div>
    <select name="level">
      <option value="highschool" {%  if member.blood == "highschool" %} selected="selected" {% endif %}>
        Medio
      </option>
      <option value="undergraduation" {%  if member.blood == "undergraduation" %} selected="selected" {% endif %}>
        Superior Incompleto
      </option>
      <option value="undergraduated" {%  if member.blood == "undergraduated" %} selected="selected" {% endif %}>
        Superior Completo
      </option>
      <option value="master" {%  if member.blood == "master" %} selected="selected" {% endif %}>
        Mestrado
      </option>
      <option value="doctor" {%  if member.blood == "doctor" %} selected="selected" {% endif %}>
        Doutorado
      </option>
    </select>
  </div>
</div>

<div class="item">
  <div>Acompanhamento</div>
  <div>
    <input type="text" name="services" placeholder="Ex: Matemática, Fisica" ,="," value="{{teacher.services}}">
  </div>
</div>

<button type="submit">Salvar</button>